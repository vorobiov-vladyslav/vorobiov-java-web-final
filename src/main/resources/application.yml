spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/university
    username: sms
    password: sms
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100

springdoc:
  swagger-ui:
    operations-sorter: method
